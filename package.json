{
    "name": "eqharmony-sdk",
    "version": "0.0.1",
    "description": "EqHarmony SDK",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "npm -ws run build",
        "clean": "npm -ws run clean",
        "test": "npm -ws run test",
        "lint": "npm -ws run lint",
        "changeset": "changeset",
        "version-packages": "changeset version",
        "release": "changeset publish"
    },
    "devDependencies": {
        "typescript": "^5.3.3",
        "ts-node": "^10.9.2"
    },
    "engines": {
        "node": ">=20",
        "npm": ">=9"
    }
}